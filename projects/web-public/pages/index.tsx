import { sum } from '@eduardofl/utils/math/sum';

export default function HomeScreen() {
    return (
      <main>
        <h1>Home screen!</h1>
        <p>Testando a função sum do package utils - sum(157, 456): { sum(157, 456) }</p>
      </main>
    );
}
